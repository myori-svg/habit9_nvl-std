# Novel Studio — 작업 체크리스트

## ✅ 완료

- [x] ManualPanel God Component 분리 (`lib/prompts.ts` + 스텝별 파일)
- [x] 메인 탭 4개로 축소 (①DQ ②구도 ③캐릭터관리 ④장면생성)
- [x] 캐릭터 관련 작업 ③ 서브탭으로 통합 (③-0 추출 / ③-1 정보 / ③-2 프롬프트 / ③-3 이미지)
- [x] SaveResultBox → 스텝별 전용 저장 박스로 분리 (구도/캐릭터정보/캐릭터프롬프트)
- [x] Copy 버튼 클릭 시 전체화면 오버레이 피드백
- [x] CharImageStep 업로드 에러 핸들링 수정 (finally 블록 타이밍 버그)
- [x] 구도 프롬프트 일괄 저장 — `===` 파싱 + DQ별 미리보기 + 한번에 저장

---

## 🔥 긴급

- [ ] **CI build 실패 이슈 해결**
  - [ ] 실패 원인 파악 (빌드 로그 확인)
  - [ ] 타입 에러 / lint 에러 수정
  - [ ] CI 통과 확인

- [ ] **이미지 저장 이슈 해결** — Firebase Spark 플랜 Storage 업로드 실패
  - [ ] 해결 방향 결정 (Blaze 업그레이드 or Cloudinary)
  - [ ] 선택한 방법으로 `lib/firestore.ts` `uploadImage` 교체
  - [ ] 업로드 → URL 저장 → Firestore 반영 E2E 테스트

---

## 🐛 버그

- [ ] **페이지 리로드 후 캐릭터 이미지 미표시**
  - [ ] `WorkPanel` `CharacterRow`에서 `imageUrl` fallback 렌더링 추가
  - [ ] `imageBase64`가 없을 때 `imageUrl`로 `<img>` 표시

- [ ] **장면 생성 시 캐릭터 이미지 미전송**
  - [ ] `/api/fetch-image` 라우트 구현 (서버에서 `imageUrl` fetch → base64 반환)
  - [ ] `WorkPanel` 장면 생성 전 `imageUrl` → base64 변환 후 API 전달
  - [ ] `app/api/fetch-image/route.docx` 스펙 확인 후 실제 route.ts로 구현

---

## 🚧 미완성 기능

- [ ] **① DQ 결과 저장 기능 구현** — 파트 이름 입력 + Gemini DQ 결과 `===` 파싱 + `novel.parts`에 저장하는 UI 없음
  - [ ] 파트 이름 입력 UI
  - [ ] DQ 결과 붙여넣기 → `===` 파싱 → 미리보기
  - [ ] 저장 시 `novel.parts`에 새 파트로 추가

- [ ] **③-3 캐릭터 이미지 탭 수정**
  - [ ] 이미지 생성 프롬프트 표시 UI 개선
  - [ ] 업로드 완료 후 미리보기 유지 / 상태 피드백 개선

- [ ] **프롬프트 수정 탭 생성**
  - [ ] 기존 PromptBox 컨테이너를 수정 가능한 textarea로 전환하는 "편집 모드" 추가
  - [ ] 수정된 프롬프트로 복사 가능하도록 처리
  - [ ] 수정 내용 저장 여부 결정 (임시 vs 영구)

- [ ] **하드코딩된 프롬프트 수정**
  - [ ] `lib/prompts.ts` 내 하드코딩된 지침 문구 검토
  - [ ] 수정이 필요한 프롬프트 식별 및 내용 업데이트
  - [ ] (선택) 프롬프트를 DB에서 관리할 수 있도록 구조 변경

- [ ] **구도 프롬프팅 조정**
  - [ ] 현재 `buildCompositionPrompt` 출력 결과 검토
  - [ ] 구도 지침 문구 및 구조 개선
  - [ ] 캐릭터 표정 / 구도 묘사 품질 향상

- [ ] **Summary Word 파일 업로드 → 파트별 저장**
  - [ ] `.docx` 파일 파싱 라이브러리 선택 (e.g. `mammoth`)
  - [ ] 업로드 UI 구현 (SetupPanel 또는 별도 탭)
  - [ ] 챕터/파트 단위로 자동 분할 후 Firestore 저장
  - [ ] GUI 방식으로 파트 체크해서 프롬프트 조립 인터페이스 구현

---

## 🤖 자동화 (가능하면)

- [ ] **Gemini API 연결 → 자동 생성 파이프라인**
  - [ ] 단계별 자동 진행 흐름 설계 (DQ → 구도 → 캐릭터 정보 → 프롬프트 → 이미지)
  - [ ] 각 단계 성공 시 자동으로 다음 단계 진행
  - [ ] 오류 발생 시에만 알림 표시, 정상 시 조용히 저장
  - [ ] 진행 상태 UI (현재 SetupPanel의 progress 방식 활용)
  - [ ] 부분 실패 시 해당 단계만 재시도 가능하도록 구현

---

## 💡 개선 아이디어

- [ ] 저장 완료 후 자동으로 다음 서브탭으로 이동
- [ ] ③-1 캐릭터 정보 저장 시 해당 캐릭터 자동 선택 (현재 매번 선택해야 함)
