# [SA-02] 이미지 Firebase 저장 함수

## 상태
완료 (AM-01 작업 중 확인 — 이미 구현되어 있었음)

## 우선순위
🔴 긴급

## 목표
`uploadImage` 함수 구현 및 UI 연결.

## 경과
`lib/firestore.ts`의 `uploadImage()`가 이미 구현되어 있었고,
`saveCharacterImage`/`saveSceneImage`/`saveDQSceneImage`(AM-01에서 추가)가
이를 사용 중. 이 티켓이 오래 방치되어 있던 것으로 보임 — 실제로는 완료된
상태라 done으로 정리.

## 완료 조건
- [x] `uploadImage` 함수 구현
- [x] SA-03과 함께 UI 연결 (CharImageStep, WorkPanel에서 사용 중)

## 의존성
- blocks: [SA-03, CH-08, MM-07] (모두 함께 정리)
